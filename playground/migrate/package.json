{
  "name": "contentful-to-strapi-migrate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Migrate a blog (posts, authors, categories, landing page) from a Contentful export into Strapi v5.",
  "scripts": {
    "migrate": "node migrate.js",
    "migrate:sample": "node migrate.js --export ./sample-data/export.json --assets-dir ./sample-data"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
